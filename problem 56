# Input
lst = list(map(int, input().split()))

freq = {}

for item in lst:
    if item in freq:
        freq[item] += 1
    else:
        freq[item] = 1

for key in freq:
    print(key, ":", freq[key])
